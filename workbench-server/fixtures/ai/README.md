# AI fixtures

Placeholder / template-style samples for offline collect CLI structure.

- `template-connector-style.txt` — synthetic connector-heavy prose (not real user content).

Production calibration should prefer platform episodes with `writing_model` metadata via `collect-ai-detect-samples.ts`.
Offline dry-run: fixtures alone may fail training guardrails (minPerClass=40); that is expected.
